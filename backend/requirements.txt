# Core
Django==5.0.6
djangorestframework==3.15.2
django-cors-headers==4.4.0

# MongoDB
# pymongo 4.9+ removed the internal _check_name symbol that mongoengine 0.28.x imports.
# Pin to <4.9 until mongoengine releases a fix or we upgrade to 0.29.x.
mongoengine==0.28.2
pymongo>=4.4,<4.9

# Auth — custom JWT (NOT django-simplejwt, which requires Django ORM)
PyJWT==2.9.0

# File storage (MinIO / S3)
django-storages==1.14.4
boto3==1.35.0

# Task queue
celery==5.4.0
redis==5.0.8

# HTTP client (used by apps/videos for YouTube API calls)
requests==2.32.3

# Google Gemini — AI lesson translation (apps/translations)
google-genai>=1.0.0,<2.0.0

# Config
python-decouple==3.8

# Production server
gunicorn==22.0.0

# Image processing (for thumbnails)
Pillow==10.4.0

# Testing
pytest==8.3.3
pytest-django==4.9.0
